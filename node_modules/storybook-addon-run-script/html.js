"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WRAPPER_ID = 'storybooks-addon-run-script';
function getOrCreate(id) {
    var elementOnDom = document.getElementById(id);
    if (elementOnDom) {
        return elementOnDom;
    }
    var element = document.createElement('div');
    element.setAttribute('id', id);
    return element;
}
function getWrapperDiv() {
    return getOrCreate(exports.WRAPPER_ID);
}
exports.withRunScript = function (runScript) { return function (storyFn) {
    var script = document.createElement('script');
    script.append(runScript);
    var wrapper = getWrapperDiv();
    var element = storyFn();
    if (typeof element === 'string') {
        wrapper.innerHTML = element;
    }
    else if (element instanceof Node) {
        wrapper.appendChild(element);
    }
    else {
        return element;
    }
    wrapper.appendChild(script);
    return wrapper;
}; };
if (module && module.hot && module.hot.decline) {
    module.hot.decline();
}
